- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."textfilemetadata"
                 add constraint "textfilemetadata_textFileId_fkey"
                 foreign key ("textFileId")
                 references "public"."textfiles"
                 ("id") on update restrict on delete restrict;
  type: run_sql
