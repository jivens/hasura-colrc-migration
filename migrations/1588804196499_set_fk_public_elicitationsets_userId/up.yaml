- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."elicitationsets"
                 add constraint "elicitationsets_userId_fkey"
                 foreign key ("userId")
                 references "public"."users"
                 ("id") on update restrict on delete restrict;
  type: run_sql
