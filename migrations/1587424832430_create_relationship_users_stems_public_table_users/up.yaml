- args:
    name: users_stems
    table:
      name: users
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: userId
        remote_table:
          name: stems
          schema: public
  type: create_array_relationship
